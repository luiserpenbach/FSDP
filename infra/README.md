# Infrastructure

Local infrastructure currently lives in the root `docker-compose.yml` and provides PostgreSQL for backend development.

Future deployment work should add environment-specific compose files or Terraform here without changing the application domain model.
